function Header() {
  return (
    <>
      <h1>Routes </h1>
      <p>
        The <b>Routes</b> component is used to define the routing configuration
        for your application. It contains a list of <b>Route</b> components that
        specify how different URLs should be matched to render specific
        components.
      </p>
      <p>
        Routes are like roads on the map. They tell the router which parts of
        your website should be shown for different website addresses (URLs).
      </p>
    </>
  );
}
export default Header;
